{
  "name": "axia-landing-page",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "pages": "npm run build && rm -rf docs && mkdir docs && cp -R dist/. docs/ && touch docs/.nojekyll && printf '%s\\n' '# Gerado automaticamente' '' 'Não edite arquivos nesta pasta.' 'Rode `npm run pages` para regenerar a partir de `src/` + `public/`.' > docs/README.md"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
